/**
 * Firebase Library Entry Point
 * 
 * Centralized exports for Firebase services.
 * Import from this file throughout the application for consistency.
 * 
 * Example usage:
 *   import { auth, db } from '@/lib/firebase';
 */

export { auth, db, app } from '../../config/firebase';
